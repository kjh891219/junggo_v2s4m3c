<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="creply">

  <insert id="reply" parameterType="CReplyVO">
    INSERT INTO creply (rno, rcomment, ctno, userid, nickname, passwd, wdate, grpno, indent, ansnum)
    VALUES((SELECT NVL(MAX(rno), 0) + 1 as rno FROM creply),
    #{rcomment}, #{ctno}, #{userid}, #{nickname}, #{passwd}, sysdate, #{grpno}, #{indent}, #{ansnum})
  </insert>

  <update id='updateAnsnum' parameterType="CReplyVO">
    UPDATE creply
    SET ansnum = ansnum + 1
    WHERE grpno = #{grpno} AND ansnum > #{ansnum}
  </update>

  <!-- 답변 1건 조회 -->
   <select id="read" resultType="CReplyVO" parameterType="int">
    SELECT rno, rcomment, ctno, userid, nickname, passwd, wdate, grpno, indent, ansnum
      FROM creply
     WHERE rno=#{rno}
  </select> 
  
  <!-- 조건 검색 -->
  <select id="listReply" resultType="CReplyVO" parameterType="int">
   SELECT rno, rcomment, ctno, userid, nickname, passwd, wdate, grpno, indent, ansnum
     FROM creply
    WHERE ctno=#{ctno}
   order by grpno DESC, ansnum ASC
  </select>
  
  <!-- 댓글 등록 시 메인글번호에 해당하는 답글 중 grpno MAX 값 가져오기-->
   <select id="getMaxgrpno" resultType="int" parameterType="int">
    SELECT nvl(max(grpno),0) grpno
    FROM creply
    WHERE ctno=#{ctno}
  </select> 
  
    <!-- 허위상품신고 답글 삭제 -->
  <delete id="delete_reply" parameterType="int">
    DELETE FROM creply WHERE rno in (  
    SELECT a.rno FROM creply a, (SELECT ctno, grpno, indent, rno, ansnum FROM creply WHERE rno = #{rno}) b
    WHERE a.ctno = b.ctno AND a.grpno = b.grpno AND a.indent >= b.indent AND a.rno >= b.rno AND a.ansnum >= b.ansnum )
  </delete>
  
</mapper>